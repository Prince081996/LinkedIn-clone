name: remote ssh command
on:
  push:
    branches: [main]
jobs:
  build:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@master
        with:
          host: "14.140.15.89"
          port: "12032"
          username: "company"
          password: "$UrY@D3^eL0pMent"
          script: |
            ls
